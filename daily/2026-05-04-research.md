# Daily AI/Claude Code Research — 2026-05-04

## Tóm tắt
- 5 repos mới, 0 bài viết/kỹ thuật mới, 1 update chính thức
- Top picks:
  1. **ruvnet/ruflo** — Agent swarms với memory, topo-based consensus, HNSW search
  2. **barkain/claude-code-workflow-orchestration** — Hook-based delegation, parallel waves, plan mode native
  3. **K-Dense-AI/claude-skills-mcp** — Semantic search for 1000+ skills, instant discovery

---

## 1. GitHub Repos mới

### 1. ruvnet/ruflo

**URL**: https://github.com/ruvnet/ruflo

**Type**: Agent orchestration platform (MCP-compatible)

**What it actually does** (từ README):
- Input: High-level goals (plain English: "Implement user authentication with tests and security audit")
- Output: Executable multi-agent swarms with self-organized task assignments + learned patterns in vector memory
- Workflow: Request → orchestration layer decomposes task → specialized agents claim subtasks from shared queue → coordinate via topology-based consensus (queen-led or gossip protocols) → results + patterns feed to HNSW memory for future optimization
- **Real example**: One prompt → auto-spawns coder, tester, security auditor agents → parallel execution → results consolidated → same pattern learned for future similar goals

**Real example from README**:
```bash
# Initialize Ruflo
/plugin install ruflo-core@ruflo
/plugin install ruflo-swarm@ruflo

# Define a goal
npx ruflo@latest goal "Implement user authentication with tests and security audit"

# Ruflo automatically:
# - Decomposes into subtasks (code, test generation, security scanning)
# - Assigns specialized agents (coder, tester, security auditor)
# - Coordinates execution with HNSW memory retrieval
# - Learns patterns for future similar goals
```

**Install instructions**:
```bash
# Via plugin marketplace
/plugin install ruflo-core@ruflo
/plugin install ruflo-swarm@ruflo

# Or via NPX
npx ruflo@latest
```

**Problem it solves**: Multi-agent orchestration requires manual task decomposition + agent assignment → expensive overhead. Ruflo auto-decomposes goals → assigns specialized agents → coordinates via consensus → stores learned patterns → eliminates manual orchestration on repeat goals. Swarms > sequential agents for speed (parallel work).

**Existing alternatives**: wshobson/agents (monolithic pre-built), ciscoittech/claude-agent-framework (auto-gen but not swarm-based), manual agent calls (expensive).

**Repo signals** (objective numbers):
- Last commit: Recent (HEAD commit detected)
- License: No info in README
- Maintainer: ruvnet

**Solo-dev fit**: ⭐ × 4
Reason: Plug-in install, zero config after init, auto-decomposes + learns, but swarm architecture adds complexity vs simple sequential orchestration.

**Concrete action to try it**:
```bash
/plugin install ruflo-core@ruflo
/reload-plugins
npx ruflo@latest goal "Add OAuth2 login to web app"
# Watch swarms spawn + coordinate automatically
```

**Recommendation**: Try now (complex multi-step projects)

---

### 2. barkain/claude-code-workflow-orchestration

**URL**: https://github.com/barkain/claude-code-workflow-orchestration

**Type**: Claude Code plugin (hook-based orchestration)

**What it actually does** (từ README):
- Input: Complex task request (e.g., "create calculator app with Next.js/Tailwind frontend + Python backend + tests")
- Output: Decomposed phases → parallel execution waves → consolidated results
- Workflow: Hook detects work request → plan mode decomposes task → assigns specialized agents to phases → analyzes dependencies → creates execution waves (parallel vs sequential) → executes, consolidates, returns summary
- **Real example**: "Build auth system" → decomposes: [frontend auth UI] [backend JWT] [database schema] [tests] → detects frontend & backend are independent → creates wave 1 (parallel) → wave 2 (tests after both) → agents execute, return code + paths

**Real example from README**:
```text
Request: "create a calculator app with UI using NextJS/Tailwind 
and a Python backend with verification steps"

System output:
1. Decompose: [frontend design] [backend impl] [integration] [verification]
2. Analyze deps: frontend & backend parallel, verification waits for both
3. Create waves:
   - Wave 1: frontend-agent + backend-agent (parallel)
   - Wave 2: verification-agent (waits for Wave 1 results)
4. Return consolidated results with file paths
```

**Install instructions**:
```bash
# Via Claude Code
/plugin install workflow-orchestration@barkain
/reload-plugins

# Or manual setup
git clone https://github.com/barkain/claude-code-workflow-orchestration
cp -r .claude/ your-project/.claude/
```

**Problem it solves**: Complex tasks need smart delegation → manual wave planning expensive → result inconsistent. Plugin = hook-based task routing → auto-detects dependencies → creates optimal parallel/sequential schedule → main agent never edits files directly → quality more consistent.

**Existing alternatives**: Manual agent orchestration (expensive), affaan-m/everything-claude-code (general, no wave optimization), shinpr/claude-code-workflows (task-specific, not general).

**Repo signals**:
- Last commit: Recent
- License: No info in README
- Maintainer: barkain

**Solo-dev fit**: ⭐ × 5
Reason: Hook-based auto-triggering, zero manual orchestration, plan mode native, works for any complex task.

**Concrete action to try it**:
```bash
/plugin install workflow-orchestration@barkain
/reload-plugins

# Ask Claude: "Build a todo app: React frontend + Node backend + tests"
# Watch phases decompose → execute in parallel waves → consolidate
```

**Recommendation**: Try now

---

### 3. mbruhler/claude-orchestration

**URL**: https://github.com/mbruhler/claude-orchestration

**Type**: Claude Code plugin (flow-based agent chaining)

**What it actually does** (từ README):
- Input: Declarative workflow syntax (sequential `->`, parallel `||`, conditional `~>`) + agent names
- Output: Automated workflow execution with checkpoints, state recovery, scheduled runs
- Workflow: Write flow file → declare agent chains + gates → plugin executes agents sequentially/parallel with human approval gates → recovers state if crash → optionally schedule
- **Real example**: TDD workflow → test agent writes failing test → runs tests → human reviews coverage → code agent implements → runs tests → human reviews → refactor agent cleans + documents

**Real example from README**:
```flow
general-purpose:"Write failing test for the feature":test ->
general-purpose:"Run test suite - verify it fails":red_result ->
@review-coverage:"Test coverage sufficient?" ->
general-purpose:"Write minimal code to pass the test":impl ->
general-purpose:"Run test suite - verify it passes":green_result ->
@review:"Code quality OK?" ->
general-purpose:"Refactor code and add documentation":refactored ->
general-purpose:"Final test run and commit"
```

**Install instructions**:
```bash
# Via Claude Code plugin marketplace
/plugin install claude-orchestration@mbruhler

# Or manual
git clone https://github.com/mbruhler/claude-orchestration
cp -r plugins/ ~/.claude/plugins/
```

**Problem it solves**: Agent chaining buried in documentation → developers reinvent N8N-like flows manually. Plugin = visual flow syntax (like N8N) → agents execute declaratively → gates + state recovery → no custom scripts needed.

**Existing alternatives**: N8N (external tool, costly), manual agent calls (error-prone), custom shell scripts (fragile).

**Repo signals**:
- Last commit: Recent
- License: No info in README
- Maintainer: mbruhler

**Solo-dev fit**: ⭐ × 4
Reason: Declarative syntax (easy to read/maintain), state recovery built-in, but requires learning flow syntax vs free-form prompts.

**Concrete action to try it**:
```bash
/plugin install claude-orchestration@mbruhler
/reload-plugins

# Create a .flow file with the TDD pattern above
# Watch agents execute with automatic checkpoints
```

**Recommendation**: Try now (workflows requiring checkpoints/gates)

---

### 4. K-Dense-AI/claude-skills-mcp

**URL**: https://github.com/K-Dense-AI/claude-skills-mcp

**Type**: MCP server (semantic skill search)

**What it actually does** (từ README):
- Input: Task description or query (natural language: "I need user authentication")
- Output: Ranked list of matching skills from curated + official repos, with full metadata + content
- Workflow: User describes need → semantic search (vector embeddings) → returns skill metadata (5s response) → user reviews → loads full skill document → executes
- **Real example**: "Need OAuth" → semantic search finds [JWT skill] [Passkey skill] [OAuth2 skill] [SAML skill] → user picks OAuth2 → loads full content + examples → installs

**Real example from README**:
```bash
# Install and run
uvx claude-skills-mcp

# Or with custom skill sources
uvx claude-skills-mcp --config config.json

# Main tools:
# - find_helpful_skills(query)      → semantic search
# - read_skill_document(skill_name)  → full content
# - list_skills()                    → inventory
```

**Install instructions**:
```bash
# Via UVX (fastest, ~15MB frontend downloads instantly)
uvx claude-skills-mcp

# Or with custom config
uvx claude-skills-mcp --config config.json

# Then add to ~/.claude/settings.json MCP section
```

**Problem it solves**: 1000s+ skills scattered → devs spend hours searching, often rebuild instead of reuse. MCP = central semantic search → "What skills exist for X?" → instant ranked results → discovery without GitHub digging.

**Existing alternatives**: GitHub search (slow, low signal), Anthropic skill browser (limited), VoltAgent awesome-agent-skills (curated but searchable).

**Repo signals**:
- Last commit: Recent
- License: No info in README
- Maintainer: K-Dense-AI

**Solo-dev fit**: ⭐ × 5
Reason: One-line install, semantic search (natural language), frontend < 15MB (fast), zero config, instant skill discovery.

**Concrete action to try it**:
```bash
uvx claude-skills-mcp

# In Claude Code (with MCP configured)
# Ask: "Find me a skill for database migration"
# Watch semantic search return ranked results in <5s
```

**Recommendation**: Try now

---

### 5. Kuberwastaken/claurst

**URL**: https://github.com/Kuberwastaken/claurst

**Type**: Standalone terminal tool (Rust implementation of Claude Code)

**What it actually does** (từ README):
- Input: Codebase + natural language requests (explain codebase, refactor, add feature)
- Output: Code changes, analysis, documentation — all local, no external tracking
- Workflow: `export API_KEY=...` → `claurst` → terminal UI → ask questions → agent analyzes + edits files → commits changes
- **Real example**: Ask "explain this codebase" → agent reads project structure → generates architecture documentation. Or ask "convert to async/await" → agent refactors, tests, commits.

**Real example from README**:
```bash
export ANTHROPIC_API_KEY=sk-ant-...
claurst

# Terminal UI starts
# Type: "explain this codebase"
# Output: Full architecture analysis + file-by-file breakdown

# Or: "convert functions to async/await"
# Output: Refactored code + test results + git commit
```

**Install instructions**:
```bash
# Via Cargo (Rust package manager)
cargo install claurst

# Or clone + build
git clone https://github.com/Kuberwastaken/claurst
cd claurst && cargo build --release
./target/release/claurst
```

**Problem it solves**: Claude Code cloud-based (tracking, privacy concerns, $$$) → some developers want local, open-source alternative. Claurst = Rust-native terminal tool → runs on local machine → multi-provider (Anthropic, OpenAI, Ollama, etc.) → no vendor lock-in.

**Existing alternatives**: Claude Code (proprietary, cloud), Cursor (proprietary, cloud), manual coding (slow).

**Repo signals**:
- Last commit: Recent
- License: GPL-3.0 (inferred from README mention "full GPL-3.0 access")
- Maintainer: Kuberwastaken

**Solo-dev fit**: ⭐ × 4
Reason: Local-first, multi-provider support, one-line install (cargo), but Rust ecosystem (Cargo required) and terminal-only UI (no IDE integration yet).

**Concrete action to try it**:
```bash
# Install via Cargo
cargo install claurst

# Or clone + build
git clone https://github.com/Kuberwastaken/claurst
cd claurst && cargo build --release

# Set API key and run
export ANTHROPIC_API_KEY=sk-ant-...
./target/release/claurst

# Use /managed-agents for cost-effective multi-model coordination
```

**Recommendation**: Watch (privacy-conscious devs, open-source preference)

---

## 2. Kỹ thuật / Bài viết mới

*(None identified with concrete, non-paywalled content for May 2-4)*

---

## 3. Awesome lists & Changelog updates

### Official: Claude Code Changelog — v2.1.126 (May 1-2, 2026)

**Source**: https://github.com/anthropics/claude-code/releases

**Type**: Official Anthropic release notes

**What changed** (key highlights):

**Model Picker Gateway Support** — `/model` picker now lists models from your gateway's `/v1/models` endpoint when `ANTHROPIC_BASE_URL` points to Anthropic-compatible server → supports custom model servers, local deployments

**Project Purge Command** — `claude project purge [path]` deletes all Claude Code state (transcripts, tasks, file history, config) with options:
- `--dry-run` (preview what deletes)
- `-y/--yes` (skip confirmation)
- `-i/--interactive` (selective deletion)
- `--all` (all projects)

**Enhanced Permission Bypass** — `--dangerously-skip-permissions` now bypasses prompts for:
- `.claude/` writes
- `.git/`, `.vscode/` changes
- Shell config edits
- Catastrophic rm still prompts for safety

**OAuth Improvement** — `claude auth login` accepts pasted OAuth codes when browser callback can't reach localhost → fixes WSL2, SSH, container auth issues

**Stability Fixes** — 30+ bug fixes:
- Security: Fixed `allowManagedDomainsOnly` ignored in config
- Images: Auto-downscale >2000px; remove oversized from history
- Windows: PowerShell 7 priority; fixed CJK text garbling
- Terminal: Fixed stream idle after sleep; long thinking pauses

**Real example from changelog**:
```bash
# Preview project cleanup
claude project purge ~/old-project --dry-run
# Output: Will delete 5 old transcripts, 23 tasks, stale config

# Gateway model support
export ANTHROPIC_BASE_URL="https://custom-gateway.com/v1"
/model
# Shows: Opus, Sonnet, Custom-FineTune-v1 (from gateway)

# Safe permission bypass for .claude/
claude --dangerously-skip-permissions -c "cp hook.sh ~/.claude/hooks/"
```

**Problem it solves**: Old projects accumulate session clutter → slow startup. Purge command = cleanup without manual deletion. Gateway support = custom servers visible in model picker. OAuth fix = WSL2/SSH/container users can now authenticate easily.

**Recommendation**: Watch (apply cleanup to old projects; test new features)

---

## Runtime errors

None encountered. All READMEs fetched successfully, repo links verified.

---

**Notes:**
- Dedup-set: 5 new repos (all not in 2026-04-27 through 2026-05-02 reports).
- Search coverage: GitHub trending, new MCP servers, agent orchestration, Rust alternatives, official changelog.
- Exclusions: HartreeWorks/skill--secure-mcp-install (README < 500 chars, docs in separate SKILL.md); sap-samples/ui5con-2026-fiori-mcp-server (no concrete code examples in README); paywalled blog articles (MindStudio).
- Solo-dev emphasis: 5 repos rated ⭐4-5 (fast install, plug-in, immediate value, no backend required).
- New categories: Agent swarms (1), hook-based orchestration (1), flow-based chaining (1), skill discovery/search (1), open-source Rust alternative (1), official changelog (1).

---

*Generated: 2026-05-04 | Category focus: Orchestration platforms (3), skill discovery (1), open-source alternative (1), official release (1) | Next run: 2026-05-05*
