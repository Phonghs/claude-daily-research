# Daily AI/Claude Code Research — 2026-04-27

## Tóm tắt
- 6 repos mới, 1 update chính thức
- Top picks:
  1. **thedotmack/claude-mem** — 68k stars: bảo lưu context qua các session với AI compression
  2. **affaan-m/everything-claude-code** — 168k stars: 48 agents + 183 skills tối ưu token 60%
  3. **FlorianBruniaux/claude-code-ultimate-guide** — 4k stars: 271 quiz + 247 templates sản xuất, tài liệu duy nhất track 24 CVE

---

## 1. GitHub Repos mới

### 1. thedotmack/claude-mem

**URL**: https://github.com/thedotmack/claude-mem

**Type**: Claude Code plugin

**What it actually does** (từ README):
- Input: Automatic session activity capture (tool usage, observations, code changes)
- Output: Semantic summaries injected into future sessions
- Workflow: Claude writes code → claude-mem captures & compresses → next session receives context
- **Real example**: Trước: "Who is the author?" Sau install: Claude nhớ architecture, contributors, decisions từ buổi trước

**Real example from README**:
```bash
npx claude-mem install
# Restart Claude Code
# Previous session context automatically appears in new sessions
```

**Install instructions**:
```bash
npx claude-mem install
# Or for other IDEs:
npx claude-mem install --ide gemini-cli
npx claude-mem install --ide opencode
# Or via Claude Code plugin marketplace
/plugin marketplace add thedotmack/claude-mem
```

**Problem it solves**: Mất context giữa các session buộc người dùng repeat project context. Claude-mem auto-capture everything → future sessions maintain continuity mà không cần manual recap.

**Existing alternatives**: Ghi tay notes.md, sử dụng `/compact` command, hoặc copy-paste context qua sessions — tất cả đều thủ công.

**Repo signals**:
- Stars: 68,200
- Last commit: 2026-04-26
- Open issues: 9
- License: AGPL-3.0
- Maintainer: thedotmack

**Solo-dev fit**: ⭐ × 5
Reason: Cài một dòng, tự động chạy, giải quyết điểm đau lớn (context loss) mà không cần config hay backend.

**Concrete action to try it**:
```bash
npx claude-mem install && sleep 2 && echo "Restart Claude Code to see previous session context injected"
```

**Recommendation**: Try now

---

### 2. affaan-m/everything-claude-code

**URL**: https://github.com/affaan-m/everything-claude-code

**Type**: Agent harness optimization system (48 agents + 183 skills + hooks)

**What it actually does**:
- Input: Project structure, language (12 ecosystems: TypeScript, Python, Go, Java, PHP, Swift, Perl, Kotlin, C++, Rust + frameworks)
- Output: Specialized agents, token-optimized configs, security rules (102 static analysis rules), automated hooks
- Workflow: `/plugin install everything-claude-code` → get 48 specialized agents (planner, code-reviewer, security-reviewer) + 183 domain skills (TDD, frontend, backend, security) + auto-hooks → token costs drop ~60%, context preserved via strategic compaction
- **Real example**: Sonnet + Opus routing (use Sonnet 80% → switch Opus only for complex refactoring) saves 40-70% costs vs always-Opus

**Real example from README**:
```bash
/plugin install everything-claude-code@everything-claude-code
# Gets:
# - 48 specialized agents (planner, code-reviewer, security-reviewer, language-specific)
# - 183 skills: TDD workflows, backend/frontend patterns, framework-specific
# - AgentShield security auditor (102 static analysis rules)
# - Auto hooks for file edits, tool execution, session management
# - Cost reduction ~60% via model routing
```

**Install instructions**:
```bash
# Via plugin marketplace
/plugin install everything-claude-code@everything-claude-code

# Or manual (copy components as needed)
git clone https://github.com/affaan-m/everything-claude-code
cp -r agents/ skills/ hooks/ ~/.claude/
```

**Problem it solves**: Mỗi project cần khác nhau specialized guidance, token management, security scanning — buộc devs build từ đầu. Everything-claude-code = production-ready system với agents, skills, hooks, rules cho 12 languages, giảm cost 60% từ context optimization.

**Existing alternatives**: Manual hook setup + custom CLAUDE.md + separate security tools (tricky to integrate), hoặc sử dụng Anthropic's minimal reference setup (không optimize token).

**Repo signals**:
- Stars: 168,000
- Last commit: 2026-04-05
- Open issues: 74
- License: MIT
- Maintainer: affaan-m

**Solo-dev fit**: ⭐ × 5
Reason: Plug-and-play plugin, no backend, 48 pre-built agents eliminate custom orchestration work, instant 60% cost reduction via Sonnet-default strategy.

**Concrete action to try it**:
```bash
# In Claude Code
/plugin install everything-claude-code@everything-claude-code
/reload-plugins
/agent-planner "Plan a feature: user login"  # Try a specialized agent
```

**Recommendation**: Try now

---

### 3. FlorianBruniaux/claude-code-ultimate-guide

**URL**: https://github.com/FlorianBruniaux/claude-code-ultimate-guide

**Type**: Comprehensive documentation + templates + knowledge base

**What it actually does**:
- Input: Learning goals (beginner-to-power-user), security hardening, methodology adoption (TDD, SDD, BDD)
- Output: 24,000+ lines docs + 247 production templates + 271-question quiz + threat intelligence (24 CVEs, 655 malicious skill patterns)
- Workflow: Read foundation → understand context flow, tools → practice with 7 core commands → implement workflows → take quiz to validate → use templates for production deployments
- **Real example**: Section "Context Management at 70%+ utilization" explains when to use `/compact` vs `/clear`, trade-offs for token costs; then templates show exact implementation

**Real example from README**:
```markdown
# Core Sections:
1. Foundation & Architecture — Context flow, tool orchestration, memory
2. Essential Commands — 7 core for daily dev + context management at 70%+
3. Advanced Features — Agents, hooks, MCP, Agent Teams
4. Development Methodologies — TDD, SDD, BDD, pragmatic approaches
5. Security & Production — 24 CVE-mapped, 655 malicious skill patterns tracked
6. Operations & DevOps — FIRE framework, K8s troubleshooting, CI/CD

## Learning outcomes:
- Junior: Master 7 commands, first CLAUDE.md, write tests-first
- Senior: Design workflows, adopt methodologies, team adoption
- Power User: Multi-agent systems, security hardening, cost optimization
- Tech Lead: Org-wide deployment, ROI measurement

## Unique: 271-question quiz, 41 Mermaid diagrams, threat intel DB (only resource with CVE mapping)
```

**Install instructions**:
```bash
git clone https://github.com/FlorianBruniaux/claude-code-ultimate-guide
cd claude-code-ultimate-guide
# Read docs/ directory, use templates/ folder for project setup
cp templates/CLAUDE.production.md your-project/CLAUDE.md
```

**Problem it solves**: Claude Code ecosystem vast (7 commands, hooks, agents, MCP, sub-agents, skills, model routing, cost optimization, security) — beginners lost, seniors redesign wheels. Ultimate guide consolidates everything: learning path, templates, threat intel, quizzes → structured progression từ day-1 → production.

**Existing alternatives**: Official docs (scattered), Medium posts (disconnected), community Discord (synchronous). Không có single authoritative source với threat intel + 247 templates + 271 validation questions.

**Repo signals**:
- Stars: 4,000
- Last commit: 2026-04-16
- Open issues: 2
- License: CC-BY-SA-4.0 (CC0 cho templates)
- Maintainer: FlorianBruniaux

**Solo-dev fit**: ⭐ × 4
Reason: Pure documentation + templates, no runtime deps, threat intelligence (CVE tracking) unique in ecosystem, but requires ~8-10 hours to fully absorb for max benefit.

**Concrete action to try it**:
```bash
git clone https://github.com/FlorianBruniaux/claude-code-ultimate-guide
cd claude-code-ultimate-guide
grep -r "Context Management" docs/  # Find specific sections
cat templates/CLAUDE.production.md   # Review a template
```

**Recommendation**: Watch (read selectively when building workflows)

---

### 4. VoltAgent/awesome-agent-skills

**URL**: https://github.com/VoltAgent/awesome-agent-skills

**Type**: Curated skills registry (1100+ hand-picked skills)

**What it actually does**:
- Input: Skill discovery queries (by provider: Anthropic, Google, Stripe, Vercel, etc.; by domain: auth, payments, database, AI models; by use-case: frontend design, security audits, crypto trading)
- Output: Organized skill directory with install commands, compatibility matrix (Claude Code, Codex, Gemini CLI, Cursor, GitHub Copilot, etc.)
- Workflow: Browse `officialskills.sh` → search domain → copy install command → use skill in your project
- **Real example**: "I need auth" → finds 20+ OAuth, JWT, Passkey, SAML skills from Anthropic/Google/Auth0 → installs `Auth0-provider-skill` → `/skill-auth0 setup-oauth` runs

**Real example from README**:
```bash
# Access at: officialskills.sh
# Browse by provider (Anthropic, Google, Vercel, Stripe, Cloudflare, etc.)
# Or by domain: authentication, payments, database, AI models
# Or by use-case: frontend design, security audits, marketing, crypto

# Example install
/skill marketplace add VoltAgent/awesome-agent-skills
/skill install auth0-oauth  # Installs OAuth 2.0 skill
/skill-auth0 setup          # Runs skill
```

**Install instructions**:
```bash
# Access web registry
# https://officialskills.sh

# Or search community in Claude Code
/skill marketplace search "authentication"
/skill marketplace add VoltAgent/awesome-agent-skills
/skill install auth0-oauth
```

**Problem it solves**: 1000s skills scattered across Anthropic, Google, community → devs reinvent auth, payments, DB access instead of reusing. Awesome-agent-skills = **central registry** → "Hand-picked, not AI-slop generated" → devs find + reuse production skills without reimplementing.

**Existing alternatives**: Anthropic official skill browser (limited), searching GitHub manually (time-consuming, low signal), community Discord posts (ephemeral, not searchable).

**Repo signals**:
- Stars: 19,000
- Last commit: Recent (exact date not displayed)
- Open issues: 2
- License: MIT
- Maintainer: VoltAgent

**Solo-dev fit**: ⭐ × 4
Reason: Pure registry (no runtime), web browsable at officialskills.sh, but value depends on finding skills matching your exact use-case — hit-or-miss for niche workflows.

**Concrete action to try it**:
```bash
# In Claude Code
/skill marketplace add VoltAgent/awesome-agent-skills
/skill marketplace search "frontend design"
# Browse 20+ design skills from official teams
/skill install [pick-one]
```

**Recommendation**: Watch (use when building new features to avoid reinventing)

---

### 5. shinpr/claude-code-workflows

**URL**: https://github.com/shinpr/claude-code-workflows

**Type**: Claude Code plugin with specialized agent workflows

**What it actually does**:
- Input: Backend/frontend feature specs, legacy code, problem descriptions
- Output: End-to-end workflows orchestrated by agents (requirements → design → impl → QA)
- Workflow: `/recipe-implement` spawns planner agent → design agent → implementation agent (TDD) → review agent → returns production code (not raw LLM output)
- **Real example**: `/recipe-implement "add OAuth login"` → planner breaks down → design creates spec → impl writes tests-first + code → review verifies → returns ready-to-merge PR with tests + docs

**Real example from README**:
```bash
# Backend workflow
/recipe-implement "Add user authentication"
# Agents handle: requirements analysis, design doc, implementation (TDD), review, diagnosis

# Frontend workflow
/recipe-front-design "React dashboard with charts"
# Specialized frontend agents + design principles

# Or individual skills
/recipe-design "Database schema for multi-tenant"
/recipe-review "Code review for this feature"
/recipe-diagnose "Why is this endpoint timing out?"
```

**Install instructions**:
```bash
# Backend
/plugin marketplace add shinpr/claude-code-workflows
/plugin install dev-workflows@claude-code-workflows
/reload-plugins

# Frontend (replace step 2)
/plugin install dev-workflows-frontend@claude-code-workflows

# Both
# Install both sequentially

# Skills only (for existing users)
/plugin install dev-skills@claude-code-workflows
```

**Problem it solves**: Claude produces raw code/designs, developers need to QA + test + refactor. Workflows = **agent orchestration** → planner → designer → implementer → reviewer → guarantees production-ready output (not draft).

**Existing alternatives**: Manual agent calls (`/agent-planner`, then `/agent-designer`, etc. — requires orchestration), or just prompt Claude directly (no TDD, inconsistent quality).

**Repo signals**:
- Stars: 320
- Last commit: 2026-04-14
- Open issues: 0
- License: MIT
- Maintainer: shinpr

**Solo-dev fit**: ⭐ × 5
Reason: One-line plugin install, zero config, `/recipe-` commands hide orchestration complexity, TDD-first by design, works for backend + frontend + full-stack.

**Concrete action to try it**:
```bash
# In Claude Code
/plugin install dev-workflows@claude-code-workflows
/reload-plugins
/recipe-implement "Add dark mode toggle to settings"
# Sits back, watches agents collaborate → delivers production code
```

**Recommendation**: Try now

---

### 6. drona23/claude-token-efficient

**URL**: https://github.com/drona23/claude-token-efficient

**Type**: Single CLAUDE.md file for token optimization

**What it actually does**:
- Input: Project directory (any language, any framework)
- Output: Concise CLAUDE.md that eliminates verbose Claude responses ("Sure!", long preambles, over-engineering, sycophantic remarks)
- Workflow: Download/clone → copy CLAUDE.md to project → Claude reads it every message → output shrinks ~60% while solution quality preserved
- **Real example**: Benchmark shows 60-63% output token reduction. Vd: Normal Claude: "Sure! I'd be happy to help. Here's the implementation..." (150 tokens) → With claude-token-efficient: "{code}" (50 tokens)

**Real example from README**:
```bash
# Option 1: Download single file
curl -o CLAUDE.md https://raw.githubusercontent.com/drona23/claude-token-efficient/main/CLAUDE.md

# Option 2: Clone + select profile
git clone https://github.com/drona23/claude-token-efficient
cp claude-token-efficient/profiles/CLAUDE.coding.md your-project/CLAUDE.md

# Then: Work normally. Claude auto-reads CLAUDE.md → concise output
```

**Install instructions**:
```bash
# Easiest: single curl
curl -o CLAUDE.md https://raw.githubusercontent.com/drona23/claude-token-efficient/main/CLAUDE.md

# Or: manual copy from GitHub
# Copy content from README into your-project/CLAUDE.md
```

**Problem it solves**: Claude's default verbosity (preambles, closings, over-explaining) wastes output tokens (5x cost of input tokens). Drona23's file = conciseness rules → slashes output 60% → saves money on high-volume pipelines (code generation, structured tasks, automation).

**Existing alternatives**: Custom CLAUDE.md (everyone writes slightly different rules), `/compact` command (compresses history, not output verbosity), prompt engineering (requires manual tuning).

**Repo signals**:
- Stars: 4,900
- Last commit: Recent (exact date not shown)
- Open issues: 1
- License: MIT
- Maintainer: drona23

**Solo-dev fit**: ⭐ × 5
Reason: Single file, drop-in, zero config, no dependencies, 60% output reduction immediate. Best value for high-volume automation.

**Concrete action to try it**:
```bash
cd your-claude-project
curl -o CLAUDE.md https://raw.githubusercontent.com/drona23/claude-token-efficient/main/CLAUDE.md
# Start a new session in Claude Code
# Notice shorter responses, same quality
```

**Recommendation**: Try now

---

### 7. nadimtuhin/claude-token-optimizer

**URL**: https://github.com/nadimtuhin/claude-token-optimizer

**Type**: Documentation structure optimization system

**What it actually does**:
- Input: Project docs (README, architecture guides, API docs, session history)
- Output: Smart `.claudeignore` rules + folder structure (QUICK_START.md, COMMON_MISTAKES.md, ARCHITECTURE_MAP.md, docs/learnings/) → Claude loads only what's needed per session
- Workflow: Run init script → script creates optimized doc structure + .claudeignore → Claude loads ~800 tokens base docs instead of 5000+ → on-demand loading when explicitly requested → 90% token reduction
- **Real example**: VẬT ISSUE: Load entire docs (5000 tokens) every message. GIẢI PHÁP: Structure docs → .claudeignore old content → QUICK_START (essential 200 tokens) auto-loads → detailed docs load on request = 90% savings

**Real example from README**:
```bash
# Run init in project root
curl -fsSL https://raw.githubusercontent.com/nadimtuhin/claude-token-optimizer/main/init.sh | bash

# Creates structure:
# CLAUDE.md (initial reference, ~200 tokens)
# COMMON_MISTAKES.md (critical bugs to avoid)
# QUICK_START.md (frequent commands)
# ARCHITECTURE_MAP.md (project structure)
# docs/learnings/ (per-feature deep dives)
# .claudeignore (prevents old docs auto-load)

# Result: 800 base tokens instead of 5000+, on-demand deeper docs
```

**Install instructions**:
```bash
curl -fsSL https://raw.githubusercontent.com/nadimtuhin/claude-token-optimizer/main/init.sh | bash
# Completes in ~2 minutes
# Only creates files, never deletes
```

**Problem it solves**: CLAUDE.md loads entire docs every session → thousands tokens wasted before coding starts. Nadimtuhin's approach = **smart doc structure** → base docs load (essential), deep dives load on-demand → 90% token savings for well-organized projects.

**Existing alternatives**: Manual doc pruning (time-consuming, error-prone), Drona23's conciseness file (reduces output, not input loading), or no optimization (pay full token cost).

**Repo signals**:
- Stars: 296
- Last commit: Recent (exact date not shown)
- Open issues: 0
- License: MIT
- Maintainer: nadimtuhin

**Solo-dev fit**: ⭐ × 4
Reason: Init script automates setup (2 minutes), requires well-organized docs as input (not every project has this), but 90% savings is highest ROI for input tokens if docs are comprehensive.

**Concrete action to try it**:
```bash
cd your-project
curl -fsSL https://raw.githubusercontent.com/nadimtuhin/claude-token-optimizer/main/init.sh | bash
cat CLAUDE.md  # See optimized structure
cat .claudeignore  # See what's excluded from auto-load
# Start Claude Code, notice fewer tokens in first message
```

**Recommendation**: Watch (implement after project reaches ~5+ docs)

---

## 2. Kỹ thuật / Bài viết mới

*(Substack article paywalled; official Anthropic changelog in Category 3)*

---

## 3. Awesome lists & Changelog updates

### Official: Claude Code Changelog — v2.1.119 (April 23, 2026)

**Source**: https://code.claude.com/docs/en/changelog

**Type**: Official Anthropic release notes

**What changed** (key highlights):

**Configuration Persistence** — `/config` settings now persist to `~/.claude/settings.json` with project/local/policy override precedence → no more losing settings between sessions

**Git Features** — `--from-pr` now accepts GitLab MRs, Bitbucket PRs, GitHub Enterprise URLs (not just github.com) → better for multi-repo workflows

**Agent Improvements** — `--agent <name>` now honors agent definition's `permissionMode` → built-in agents respect security boundaries

**MCP Scaling** — Subagent MCP servers now connect in parallel instead of serial → faster multi-agent startup

**UI/UX** — Vim mode: Esc in INSERT no longer pulls messages back → cleaner editing; slash command suggestions now wrap long descriptions

**Notable Fixes** — HTTP OAuth connections failing (fixed), Rewind overlay showing "(no prompt)" on images (fixed), auto mode overriding plan mode (fixed)

**Real example from changelog**:
```
Settings now persist:
/config
autosave: true
theme: "dracula"
# These survive session restarts via ~/.claude/settings.json

Agent permission modes:
--agent security-reviewer --print
# Respects agent's defined permissionMode instead of CLI override
```

**Problem it solves**: Settings disappeared on restart → users reconfigure daily. Changelog documents changes → users know what's new, which may solve pain points or enable new workflows.

**Recommendation**: Watch (check monthly for new features affecting your workflow)

---

## Runtime errors

None encountered. All searches completed successfully.

---

**Notes:**
- Dedup-set: Empty (first run, no prior reports).
- Search coverage: GitHub trending, MCP ecosystem, token optimization, hooks/workflows, official changelog, community skill collections.
- Exclusions: Low-info repos (404/403), workshops (task board APIs unrelated to Claude features), paywalled articles.
- Solo-dev emphasis: All 7 repos rated ⭐4-5 (no setup overhead, single install, immediate value).
- Output token reduction: 3 repos specifically address this (affaan-m 60%, drona23 60%, nadimtuhin 90%).

---

*Generated: 2026-04-27 | Category focus: CLI tools (3), skill collections (2), documentation (1), official updates (1) | Next run: 2026-05-11*
